apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vcenteremu

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml

images:
  - name: vcenteremu
    newName: image-registry.openshift-image-registry.svc:5000/vcenteremu/vcenteremu
    newTag: latest
